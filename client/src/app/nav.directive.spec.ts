import { NavDirective } from './nav.directive';

describe('NavDirective', () => {
  it('should create an instance', () => {
    const directive = new NavDirective(this);
    expect(directive).toBeTruthy();
  });
});
